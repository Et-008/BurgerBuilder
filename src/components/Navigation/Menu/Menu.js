import React from "react";
import "./Menu.css";

let menu = (props) => (
  <div onClick={props.clicked} className="Menu">
    <div></div>
    <div></div>
    <div></div>
  </div>
);

export default menu;
